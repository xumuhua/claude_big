# llm_monthly 链路处置留痕（BIG-LLM1，2026-09-12）

## 链路历史

- **2026-08-05 建链**（commit `e264149`）：豆包月频 LLM 分析管线。定位结论：**交易指导层证伪**（v1 评级与量化信号信息同源零增量），**观测报告层保留**（157/157 月覆盖）。产出 `output/llm_monthly/{YYYYMM}.json/.md`。
- **2026-08-06 v2 重构**（commit `70b2592`）：改为阶段判定（筑底/上升/冲高/下降/震荡）× 宏观逻辑 × 逻辑强度；回测采纳 phase-sell（只卖不买）形态。v2 全量重跑后产物齐到 202608（asof=20260804，旧 10 票池口径）。
- **池子三次瘦身**：原油双 ETF 退市移出（20260814，`191bbf9`）、513180 恒生科技哥哥指令清仓移池（20260908，`5e82043`）、588060 科创板移池（20260908，`2233c99`）→ 现 9 票池。
- **2026-08-07 后事实停更**：8/31 月末 `rotation_live.ensure_monthly_llm` 未触发当月月报（D4v3 战役期间标记"停更盯梢"）；停更期间机检按"最新月报距今 ≤35 日历天"口径持续报 HIGH（202608.json 内容戳 20260804，9/8 起超阈，9/9-9/11 连续挂账）。
- **2026-09-12 哥哥拍板**："需要～处理下吧～" → BIG-LLM1 派单处置（亦菲 → quant）。

## 本次处置（2026-09-12）

1. **一次性补生成 202609 月报**：`monthly_llm_analysis.py --months 202609` 补跑成功，
   产出 `202609.json + 202609.md`（asof=20260911，version=2，豆包 `bot-20250308205057-5kjf4`）。
   - **口径说明**：本次为**事件级补跑**（asof=9/11 收盘，非自然月末 9/30），严格只用
     9/11 及之前可见数据，无 lookahead；与月末当日生成口径的差异仅为特征截点，方法论一致。
     9 月自然月末（9/30）将由 rotation_live 月末机制自动生成正式月末版覆盖（同文件名，
     仅 version=2 覆盖 version=2）。
   - 内容覆盖当前 9 票池全部标的；链式上文读 202608.json，LLM 自校验已识别池切换
     （"已调出油气类标的不影响当前判断"）。
2. **机检口径结构性修复**（`claude/scripts/deliverable_freshness_check.py::check_monthly_llm_chain`）：
   - 旧口径：最新月报内容戳距今 ≤35 日历天 → **结构性挂账**（月报 asof=月初时每月 1-10 号
     必报 HIGH；202608 月报 asof=8/04，9/1 即 28 天、9/8 起 HIGH）。
   - 新口径：**月末锚点制**——锚点=最近月末（由 `y-data.csv` 尾部 1500 收盘 bar 推得最近
     两个交易日判同月/跨月：同月取上月最后自然日，跨月取数据最新交易日），最新月报 asof
     落后锚点 >7 天才 HIGH。月报是月度节奏产物，新鲜度应回答"是否覆盖最近月末"而非"距今几天"。
   - 保守性：只改本检查项锚点逻辑，检查项本身保留；锚点推导失败或产物缺 asof 自动回退
     旧 35 天口径；长假场景（如 10/1-10/8）由 7 天宽容窗吸收——月末机制 + 月初补漏机制
     （M3 修复，`88b9990`）双失效才会触发，属应报警的真实故障。
3. **告警验证**：改后 `deliverable_freshness_check.py --strict` 全量通过
   （HIGH=0 LOW=0，exit=0），非压一天——下月 10/1 起锚点=9/30，只要月末机制或
   月初补漏机制其一存活即不再误报。

## 后续口径

**继续月度更新**（不转档案、不下线）。保障机制两层：
- `rotation_live.ensure_monthly_llm`：月末交易日当月月报缺失 → 当晚自动生成（既有机制，本次未动）；
- 月初 ≤10 天缺失 → 月初补漏机制兜底，失败走 live_alert 显式告警（M3 修复，既有）；
- 机检月末锚点口径监督：某月末未生成，次月 8 号后锚点切换即报 HIGH。

历史产物 202606-202608（旧 10 票池口径）全部保留归档，未删任何产物。

## 与 rotation_backtest / rotation_live 的依赖关系（已核实源码）

- `rotation_backtest.bt()`：`llm_mode="off"` 时完全不读 llm_monthly；开启时按 `asof<=当日`
  时间线软消费，**目录缺文件静默跳过**（try/except pass），停更不阻塞回测。
- `rotation_live.py`：月报仅用于 ① 事件级 LLM 的链式上文（prev，缺则为 None 软降级）
  ② 月末/月初自生成触发点。B 轨决策主链路（打分/闸门/熔断）不依赖月报。停更期
  rotation_live 每晚照常出指导文件（git log 日更不断），证实软降级成立。
- 真正受停更影响的是**观测层**：事件级 LLM 失去最新链式上文、月末复盘缺依据——即机检
  报 HIGH 的原始动机，本次补生成 + 口径修订后恢复。
- 本次处置**未触碰** `rotation_backtest.py` / `rotation_live.py` 任何逻辑（硬约束）。
