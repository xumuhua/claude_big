# llm_monthly 链路处置留痕（BIG-LLM1，2026-09-12）

## 链路历史

- **2026-08-05/06 建立**：月度豆包 LLM 分析管线 v2（`scripts/monthly_llm_analysis.py`），
  产出 `output/llm_monthly/{YYYYMM}.json/.md`。用途：每月末对轮动池做宏观阶段判定
  （筑底/上升/冲高/下降/震荡）+ 宏观逻辑归因，作为 `rotation_live` 事件级分析
  （`llm_events`）的**链式上文**（上月判定喂给本月 prompt）。
- **2026-08-22 后实质停更**：8 月月报以 `--dates 20260804` 事件级方式生成于 8/4，
  asof=20260804；8/31 月末未触发补跑（D4v3 战役期间标记"停更盯梢"），9 月起
  交付件机检按"距今≤35 日历天"口径持续报 HIGH（9/9-9/11 连续 3 天挂账）。
- **2026-09-12 哥哥拍板**："需要～处理下吧～" → BIG-LLM1 派单处置。

## 本次处置（2026-09-12）

1. **一次性补生成 202609 月报**：`202609.json/.md` 已产出（asof=20260911，
   version=2，覆盖全部 9 只在池标的）。口径说明：9 月月报在 9/12 生成，
   以最近交易日 9/11 收盘特征为证据，属月初补跑口径（与月末当日生成口径的差异
   仅为特征截点，方法论一致）。
2. **机检口径结构性修复**（`claude/scripts/deliverable_freshness_check.py`
   `check_monthly_llm_chain`）：原口径"最新月报距今≤35 日历天"对月度节奏产物是
   结构性误报——月报 asof=月末，每月 1-10 号必然超 35 天。改为**月末锚点口径**：
   锚点=由 y-data.csv 尾部推得的最近月末交易日，最新月报 asof 落后锚点 >7 天
   才报 HIGH；锚点不可用或产物缺 asof 时回退旧口径（保守）。只调阈值/口径，
   未删检查项。
3. **告警验证**：改后跑机检 HIGH=0 LOW=0，月度LLM链 ✓
   （"最新 202609 asof=20260911 (锚点 20260831, 覆盖最近月末)"）。

## 后续口径

- **继续月度更新**（不转档案/下线）。保障机制有两层：
  - `rotation_live.ensure_monthly_llm`：月末交易日当月月报缺失时自动生成（已存在，逻辑未动）；
  - 机检新口径：若某月末未生成，次月 8 号后锚点切换即报 HIGH，可及时发现。
- 202606-202608 历史月报全部保留归档，未删任何产物。

## 与 rotation_backtest / rotation_live 的依赖关系（已核实）

- `rotation_backtest.py`：月报仅作为可选 LLM 因子输入，缺失时软降级
  （prev=None 链式上文为空，首轮分析正常进行），**不受停更影响**。
- `rotation_live.py`：`ensure_monthly_llm`（月末补生成）与 `ensure_event_llm`
  （事件级分析读上月月报作链式上文）均对月报缺失做了 `os.path.exists` 判空 +
  prev=None 软降级。月报缺失只会降低事件级分析的上下文质量，不会报错中断。
- 本次处置**未触碰** `rotation_backtest.py` / `rotation_live.py` 任何逻辑（硬约束）。
